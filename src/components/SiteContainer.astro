---
export interface Props {
  class?: string
}

const { class: className = "" } = Astro.props
---

<div
  class={`site-container mx-auto flex min-h-svh w-full max-w-[640px] flex-col px-6 pt-32 pb-24 sm:px-8 sm:pt-40 lg:pt-48 lg:pb-32 ${className}`}
>
  <slot />
</div>
