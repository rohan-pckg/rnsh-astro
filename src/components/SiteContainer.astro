---
export interface Props {
  class?: string
}

const { class: className = "" } = Astro.props
---

<div
  class={`site-container mx-auto flex min-h-svh w-full max-w-[640px] flex-col px-6 pt-32 pb-20 sm:px-8 sm:pt-36 lg:pt-40 ${className}`}
>
  <slot />
</div>
