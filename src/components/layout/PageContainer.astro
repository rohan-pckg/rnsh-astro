---
import SiteContainer from "@/components/SiteContainer.astro"

export interface Props {
  class?: string
}

const { class: className = "" } = Astro.props
---

<SiteContainer class={className}>
  <slot />
</SiteContainer>
